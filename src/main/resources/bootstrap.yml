management:
    endpoint:
      refresh:
        enabled: true
      restart:
        enabled: true
spring:
    application:
        name: qaradiator-backend
    cloud:
        config:
            uri: http://localhost:8888/
            fail-fast: true
